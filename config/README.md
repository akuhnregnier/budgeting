# Configure the Budgeting App

Insert a file called `categories.yaml` in this folder with the following structure to enable categorisation of transactions:
```yaml
category1:
- pattern1
- pattern2
```
Matching is performed case-insensitively.
